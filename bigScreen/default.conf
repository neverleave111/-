# 示例：bigscreen/default.conf
server {
    listen 80;  # 容器内端口（与 docker-compose 映射一致）
    server_name localhost;
    root /usr/share/nginx/html;  # 前端打包文件目录
    index index.html;

    # 关键配置：所有请求转发到 index.html，由前端路由处理
    location / {
        try_files $uri $uri/ /index.html;
    }
}